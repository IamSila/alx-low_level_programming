/*authored by Sila Mulingi*/

#include <time.h>
#include <stdio.h>
#include <stdlib.h>

/**
 * main - entry point for the prog
 *
 * Return: is always 0
 *
 */

int main(void)
{
int a;
a = 0;

while (a <= 9)
{
	printf("%d", a);
	a++;
}
putchar('\n');

return (0);
}
